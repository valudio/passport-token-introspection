{
  "name": "passport-token-introspection",
  "version": "1.0.1",
  "description": "TPassport strategy for authenticating when using a token instrospection endpoint.",
  "typings": "dist/index.d.ts",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "version": "npm run build",
    "postversion": "npm publish",
    "rls": "npm --no-git-tag-version version",
    "rls:patch": "npm run rls -- patch",
    "rls:minor": "npm run rls -- minor",
    "rls:major": "npm run rls -- major"
  },
  "author": "Valudio <development@valudio.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/valudio/passport-token-introspection"
  },
  "dependencies": {
    "@types/passport-strategy": "^0.2.30",
    "passport-strategy": "^1.0.0"
  },
  "devDependencies": {
    "rimraf": "^2.5.4",
    "tslint": "^3.15.1",
    "typescript": "^2.0.7"
  }
}
